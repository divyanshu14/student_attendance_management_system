
import 'package:flutter/material.dart';
import 'package:sams/models/user_info.dart';

class StudentCoursePage extends StatelessWidget {
  final Course course;
  StudentCoursePage({@required this.course});
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}